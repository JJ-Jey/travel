<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.nowon.domain.mapper.MemberMapper">
	<insert id="save">
		insert into member(email, password, name, birth, address)
		values(#{email}, #{password}, #{name}, #{birth}, #{address})
	</insert>
</mapper>